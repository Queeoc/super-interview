# biz_agent 重构开发路线图

> 目标：将当前以 `RAG + AIOps Agent` 为核心的 `biz_agent`，重构为一个面向面试场景的 AI 原生平台，并让这份文档可以直接作为后续 AI 辅助编码工作的执行指南。

> 配套规范：开始任何开发前，先遵守 [AGENTS.md](</D:/学习/biz_agent/AGENTS.md>)。

---

## 一、这份文档怎么用

这不是一份“概念介绍”，而是一份**可执行的重构路线图**。后续不管是你亲自开发，还是让 AI 辅助编码，都建议遵循以下使用方式：

1. 只做一个阶段或一个子任务，不要一次跨越多个阶段。
2. 每个子任务都要先确认：
   - 目标文件
   - 输入输出
   - 依赖项
   - 验收标准
3. 每次让 AI 写代码时，只给它当前子任务的上下文，不要把整份路线图一次性压给它。
4. 每做完一个子任务，都要进行最小验证：
   - 能否运行
   - 接口是否通
   - 数据是否落库
   - 日志是否可读
5. 旧代码允许并存，但新功能必须优先落在目标架构目录，不允许继续堆在 `aiops` 业务路径上。

---

## 二、重构目标与范围

### 2.1 当前项目现状

当前 `biz_agent` 已经具备以下基础能力：

- FastAPI 入口与基础 API 壳
- LangChain / LangGraph Agent 能力
- Milvus 向量化与检索链路
- MCP 工具接入
- AIOps 的 `Plan-Execute-Replan` 工作流样板
- 简单静态前端页面

当前项目的优势是：**底层 Agent / RAG / Tool Calling 骨架已经存在**。

当前项目的不足是：

- 没有真正的“面试领域模型”
- 没有 PostgreSQL 持久化结构
- 没有 Redis 会话缓存 / 异步任务体系
- 没有 Skill 系统
- 没有统一评估引擎
- 没有简历分析与报告导出
- 现有 `chat` / `aiops` 更像通用样板，不是最终业务系统

### 2.2 重构后的目标系统

目标态是一个 AI 面试平台，核心能力包括：

- 模拟面试会话管理
- Skill 驱动的方向化出题
- RAG 驱动的知识检索与追问
- 统一评估引擎
- 简历上传、解析、分析
- 面试报告导出
- Redis 支撑的缓存、异步任务、限流
- 多 LLM Provider 运行时切换

### 2.3 本次重构不追求一步到位

本路线图采用“分阶段重构”策略：

- 先完成底层基础设施补齐
- 再完成知识库与 Skill 系统
- 再完成文字面试主流程
- 最后补齐评估、简历、报告、多 Provider、限流、前端

这样做的目的，是降低一次性大改带来的风险，也更适合 AI 辅助编码逐步推进。

---

## 三、重构总原则

### 3.1 先立骨架，再搬业务

不要先急着复制 Java 项目的业务逻辑。正确顺序是：

1. 搭建 Python 目标分层结构
2. 补齐 PostgreSQL / Redis / Storage / 配置体系
3. 再逐步迁移 Skill、RAG、面试、评估等业务

### 3.2 保留底层能力，替换业务外壳

当前仓库中这些内容优先复用：

- `app/agent/mcp_client.py`
- `app/services/document_splitter_service.py`
- `app/services/vector_index_service.py`
- `app/services/vector_store_manager.py`
- `app/core/milvus_client.py`
- `app/core/llm_factory.py`

这些模块代表的是**可复用基础能力**。

以下内容不应继续作为面试系统主路径扩展：

- `app/services/aiops_service.py`
- `app/agent/aiops/*`
- 当前以 `MemorySaver + localStorage` 为主的会话持久化方案

### 3.3 每个阶段都必须可运行

每个阶段结束后，项目都应该至少满足：

- 能启动
- 关键依赖可连接
- 新增接口可调用
- 日志可追踪

不能接受“最终会重构好，现在先堆着”的做法。

### 3.4 AI 辅助编码的最佳粒度

建议后续把开发任务切成以下粒度再交给 AI：

- 单个目录或单个模块
- 一个 Service + 它依赖的 Model / Repository
- 一个 LangGraph workflow
- 一个中间件
- 一个异步 Producer / Consumer 对

不建议一次让 AI “把整个面试平台全部实现完”。

---

## 四、目标目录与模块落位

建议将重构目标明确为如下结构：

```text
app/
├── api/
├── middleware/
├── services/
├── agent/
│   ├── interview/
│   └── evaluator/
├── tools/
├── repositories/
├── core/
├── models/
└── utils/

skills/
knowledge_base/
prompts/
scripts/
tests/
```

### 4.1 现有目录处理策略

| 现有目录 | 处理策略 |
|----------|----------|
| `app/api/` | 保留，逐步新增 interview / resume / skill / provider API |
| `app/services/` | 保留，新增业务服务，老的 aiops 服务不再扩展 |
| `app/agent/aiops/` | 保留为样板，禁止新增面试逻辑 |
| `app/tools/` | 保留，扩展为面试工具集 |
| `static/` | 作为过渡期前端保留 |
| `uploads/` | 可继续保留，后续迁移为对象存储 + 本地临时目录 |
| `volumes/` | 开发期保留 |

### 4.2 需新增目录

- `app/repositories/`
- `app/middleware/`
- `app/agent/interview/`
- `app/agent/evaluator/`
- `skills/`
- `knowledge_base/`
- `prompts/`
- `scripts/`
- `tests/`

---

## 五、开发阶段总览

建议拆成 10 个阶段：

1. Phase 0：重构准备与基线清理
2. Phase 1：基础设施补齐
3. Phase 2：领域模型与持久化骨架
4. Phase 3：知识库与 RAG 服务重构
5. Phase 4：Skill 系统落地
6. Phase 5：文字面试主流程
7. Phase 6：统一评估引擎与报告
8. Phase 7：简历模块
9. Phase 8：异步任务、多 Provider、限流与系统增强
10. Phase 9：前端适配与切换
11. Phase 10：收尾、迁移、压测与文档完善

下面按阶段详细展开。

---

## 六、Phase 0：重构准备与基线清理

### 6.1 阶段目标

在不破坏现有可运行能力的前提下，建立一套适合后续持续重构的基础规则和目录基线。

### 6.2 本阶段要实现什么

- 确认目标架构文档
- 固化编码规范
- 建立重构过程中的模块边界
- 增加基础目录占位
- 为后续数据库 / Redis / Storage 接入预留配置结构

### 6.3 如何实现

建议子任务如下：

#### 子任务 A：补齐文档基线

目标：

- 确保以下文档存在并可用：
  - `AGENTS.md`
  - 本文档 `重构开发路线图.md`

注意点：

- 文档不是摆设，后续 AI 编码都必须参考
- 若实际实现偏离文档，优先更新文档后再继续开发

#### 子任务 B：建立目标目录

建议新增空目录或占位文件：

- `app/repositories/__init__.py`
- `app/middleware/__init__.py`
- `app/agent/interview/__init__.py`
- `app/agent/evaluator/__init__.py`
- `skills/.gitkeep`
- `knowledge_base/.gitkeep`
- `prompts/.gitkeep`
- `scripts/.gitkeep`
- `tests/__init__.py`

注意点：

- 不要在此阶段写业务逻辑
- 目标是让后续模块“有地方可放”

#### 子任务 C：重构配置骨架

建议先把 `app/config.py` 拆成嵌套 settings 风格，至少预留：

- `app`
- `llm`
- `milvus`
- `postgres`
- `redis`
- `storage`
- `interview`
- `rag`

注意点：

- 不要求本阶段就全部接上真实依赖
- 但配置结构要先稳定下来，避免后面业务写一半再返工

### 6.4 本阶段验收标准

- 项目仍能启动
- 新目录结构已具备
- 文档可指导后续 AI 编码
- 配置文件具备目标态骨架

### 6.5 本阶段风险

- 为了“快”，继续把新逻辑塞到旧目录
- 配置仍旧散乱，导致后续接入 PostgreSQL / Redis 时再次返工

---

## 七、Phase 1：基础设施补齐

### 7.1 阶段目标

把当前项目从“只有 Milvus 和 FastAPI”的轻量样板，提升为“具备业务系统底座”的应用。

### 7.2 本阶段要实现什么

- PostgreSQL 接入
- Redis 接入
- Storage 接入抽象
- 统一异常处理
- 统一日志初始化
- 本地开发依赖编排

### 7.3 如何实现

#### 子任务 A：新增数据库基础设施

新增文件建议：

- `app/core/database.py`
- `app/models/base.py`

实现内容：

- 创建 SQLAlchemy async engine
- 创建 `async_sessionmaker`
- 定义 `Base`
- 提供依赖注入方法，例如 `get_db_session()`

注意点：

- 只做连接与 session 管理，不写业务表
- 不要把 ORM 实体和数据库连接逻辑混在一起

#### 子任务 B：新增 Redis 客户端

新增文件建议：

- `app/core/redis_client.py`

实现内容：

- 初始化异步 Redis 客户端
- 提供连接、ping、基础 key/value、stream、eval 能力

注意点：

- 统一用一个 Redis 封装，不要每个模块自己创建 Redis 连接
- 提前考虑：
  - 普通缓存
  - Stream
  - Lua 限流

#### 子任务 C：新增 Storage 客户端抽象

新增文件建议：

- `app/core/storage_client.py`

实现内容：

- 先定义统一接口：
  - `upload_file`
  - `download_file`
  - `delete_file`
  - `get_public_url`
- 第一版可以先支持本地文件系统 + 预留 S3/MinIO 实现

注意点：

- 不要直接在业务代码里写 S3 SDK
- 后续简历与 PDF 导出都要走这一层

#### 子任务 D：统一异常处理

新增文件建议：

- `app/utils/exceptions.py`
- `app/middleware/error_handler.py`

实现内容：

- 定义 `BusinessException`
- 定义错误码枚举或错误码常量
- 定义全局异常处理器

注意点：

- API 层不应散落大量 `try/except Exception`
- 必须把业务异常和技术异常区分开

#### 子任务 E：日志与生命周期统一

优化文件建议：

- `app/main.py`
- `app/utils/logger.py`

实现内容：

- 应用启动时统一初始化：
  - Loguru
  - PostgreSQL
  - Redis
  - Milvus
- 健康检查中体现关键依赖状态

注意点：

- 不要只有 Milvus 健康检查
- 至少要检查 PostgreSQL / Redis / Milvus

#### 子任务 F：补齐开发环境编排

建议新增或调整：

- `docker-compose.dev.yml`

内容建议：

- PostgreSQL
- Redis
- Milvus
- MinIO/RustFS（可选）

注意点：

- 开发环境编排尽量最小闭环
- 不要求一上来就把完整生产拓扑做复杂

### 7.4 本阶段验收标准

- 应用可连接 PostgreSQL / Redis / Milvus
- `/health` 能看到依赖状态
- 配置通过 `.env` 生效
- 没有在业务代码里散落连接初始化

### 7.5 实现时的注意要点

- 不要一边接入数据库一边定义业务表，先稳定底层
- 不要让 Redis / Storage 接入带入业务逻辑
- 所有第三方客户端都必须通过 `core/` 统一暴露

---

## 八、Phase 2：领域模型与持久化骨架

### 8.1 阶段目标

建立“面试系统”的结构化数据基础，让系统摆脱只依赖内存和本地前端缓存的状态管理。

### 8.2 本阶段要实现什么

- SQLAlchemy 实体模型
- Repository 层
- 基础 CRUD 与查询能力
- 会话持久化骨架

### 8.3 如何实现

建议优先建立以下模型：

- `InterviewSessionEntity`
- `InterviewQuestionEntity` 或 `questions_json` 方案
- `InterviewAnswerEntity`
- `InterviewReportEntity`
- `ResumeEntity`
- `ResumeAnalysisEntity`
- `KnowledgeBaseEntity`
- `RagChatSessionEntity`
- `RagChatMessageEntity`
- `LlmProviderEntity`

#### 子任务 A：定义实体

新增文件建议：

- `app/models/interview.py`
- `app/models/resume.py`
- `app/models/knowledge.py`
- `app/models/provider.py`

实现内容：

- 表结构定义
- 基础索引
- 枚举字段
- 时间戳字段

注意点：

- 不追求一步到位的表字段最全版本
- 先支撑 MVP 主流程
- 推荐有些复杂对象先 JSON 化存储，但要保留未来可拆分空间

#### 子任务 B：建立 Repository

新增文件建议：

- `app/repositories/interview_repository.py`
- `app/repositories/resume_repository.py`
- `app/repositories/knowledge_repository.py`
- `app/repositories/provider_repository.py`

实现内容：

- 基础增删改查
- 常用查询封装
- 会话按状态查找
- 报告按 session 查询

注意点：

- 查询逻辑不要写在 Router / Tool 中
- Repository 负责“数据访问”，不负责“业务决策”

#### 子任务 C：建立持久化服务

新增文件建议：

- `app/services/interview_persistence_service.py`
- `app/services/resume_persistence_service.py`

实现内容：

- 负责组装 Repository 调用
- 负责持久化相关的业务动作

注意点：

- 持久化服务不是“重复包一层”，而是承接数据一致性和聚合写入

### 8.4 本阶段验收标准

- 可以创建并查询面试会话
- 可以保存答案与报告占位
- 可以创建简历和知识库记录
- 不再依赖 `MemorySaver` 作为唯一事实源

### 8.5 注意要点

- 不要急着写复杂业务逻辑，先把事实数据结构立住
- 旧的 `MemorySaver` 可暂时保留用于调试会话流，但必须逐步退出主业务路径

---

## 九、Phase 3：知识库与 RAG 服务重构

### 9.1 阶段目标

把当前偏“通用知识检索”的 RAG 能力，重构成服务于面试业务的知识基础设施。

### 9.2 本阶段要实现什么

- 知识库实体与文件管理联动
- 文档上传 -> 解析 -> 分块 -> 向量化 -> 入库完整链路
- 支持按知识库查询
- 支持题库 / reference / rubric 分类
- 为 Skill 系统提供检索基础

### 9.3 如何实现

#### 子任务 A：重构文件入库逻辑

涉及现有可复用模块：

- `app/services/document_splitter_service.py`
- `app/services/vector_index_service.py`
- `app/services/vector_store_manager.py`

需要新增或改造：

- `app/services/knowledge_service.py`
- `app/services/rag_service.py`
- `app/api/knowledge.py`

实现目标：

- 上传文件时先生成 `KnowledgeBaseEntity`
- 再进行解析与向量化
- 文档元数据中要保存：
  - `knowledge_base_id`
  - `source_type`
  - `category`
  - `skill_id`（可选）

注意点：

- 当前 `vector_index_service` 只按文件路径入库，后续要转为“知识库记录 + 文件记录”驱动
- 不要继续只依赖 `_source` 作为检索组织方式

#### 子任务 B：支持按知识库查询

新增能力：

- 指定知识库 ID 检索
- 支持多知识库联合检索
- 支持按分类过滤，例如：
  - `interview_questions`
  - `reference_knowledge`
  - `rubrics`

注意点：

- 这一步是后续面试 Agent 做精准知识注入的关键
- 检索结果必须能溯源到知识库和文件

#### 子任务 C：引入 query rewrite 与检索策略

建议实现：

- `prompts/rag/query_rewrite.st`
- `rag_service.py` 中的查询改写逻辑
- TopK / score threshold 动态策略

注意点：

- 不要一开始做过度复杂的检索优化
- 第一版只要做到：
  - rewrite 可开关
  - 参数可配置
  - 空命中有兜底

### 9.4 本阶段验收标准

- 文件可上传并建立结构化知识库记录
- 文档可以成功向量化入库
- 能按知识库维度检索
- 检索结果具备稳定元数据

### 9.5 注意要点

- 当前 `uploads/` 只是临时目录，不要把它误当成持久化系统
- 文档解析逻辑要与“知识库记录管理”分离
- 检索层与回答层要分开，不要把回答写死在 Tool 内部

---

## 十、Phase 4：Skill 系统落地

### 10.1 阶段目标

把“面试方向”从硬编码逻辑变成可配置的 Skill 体系。

### 10.2 本阶段要实现什么

- `skills/` 目录结构
- `SKILL.md + skill.meta.yml` 加载器
- Skill 列表与详情接口
- 预设 Skill 的首批资源落地
- Skill 与 reference / tools / question categories 绑定

### 10.3 如何实现

#### 子任务 A：建立 Skill 文件体系

建议新增目录：

- `skills/java-backend/`
- `skills/python-backend/`
- `skills/frontend/`
- `skills/algorithm/`
- `skills/system-design/`
- `skills/_shared/references/`

实现内容：

- 将 Java 项目中已有的 `SKILL.md` / `skill.meta.yml` 迁移为 Python 项目资源

注意点：

- 先迁移最小集合，不必一次迁完全部 10+ Skill
- 第一批建议：
  - `java-backend`
  - `python-backend`
  - `algorithm`

#### 子任务 B：实现 Skill Loader

新增文件建议：

- `app/services/skill_service.py`
- `app/api/skill.py`
- `app/models/skill.py`（如需要）

实现内容：

- 扫描 `skills/*/SKILL.md`
- 解析 front matter / meta yaml
- 组装标准 Skill DTO

注意点：

- Skill loader 只负责“加载与解析”，不负责“出题”
- 解析失败时要有清晰错误提示

#### 子任务 C：固化首批预设 Skill 清单

建议能力：

- 提供固定的 Skill ID 列表
- 保证首批 Skill 可以稳定加载、展示与查询
- 后续新增方向时通过新增 Skill 目录扩展，而不是改核心逻辑

注意点：

- 第一版不做 JD 解析或动态 Skill 分类
- 先把“预设 Skill 可配置化”跑通即可
- 第一批建议继续聚焦：
  - `java-backend`
  - `python-backend`
  - `algorithm`

#### 子任务 D：建立 Skill -> Reference / Tool 关系

重点能力：

- 每个 Skill 可以声明：
  - categories
  - references
  - enabled tools
  - question preferences

注意点：

- 这一步是让 Skill 从“静态 Prompt”升级为“动态 Agent Skill”的关键

### 10.4 本阶段验收标准

- 可以列出所有 Skill
- 可以获取单个 Skill 详情
- 可以从 Skill 构建 reference section
- 第一批预设 Skill 可以稳定加载并通过接口返回

### 10.5 注意要点

- Skill 是配置，不是代码分支
- 不能每加一个方向就去改 `question_service.py` 核心逻辑
- 当前阶段不要引入 JD 解析，先聚焦预设 Skill 体系落地

---

## 十一、Phase 5：文字面试主流程

### 11.1 阶段目标

实现 MVP 版文字面试主链路：创建会话、出题、答题、追问、下一题、结束。

### 11.2 本阶段要实现什么

- 面试会话 API
- 面试状态模型
- 面试 Planner / Executor / Replanner
- 会话持久化与缓存
- SSE 流式事件

### 11.3 如何实现

#### 子任务 A：定义面试状态与 DTO

新增文件建议：

- `app/agent/interview/state.py`
- `app/models/interview.py` 中的 Pydantic 请求响应对象

需要定义：

- `CreateInterviewRequest`
- `InterviewSessionDTO`
- `SubmitAnswerRequest`
- `SubmitAnswerResponse`
- `InterviewState`

注意点：

- 领域状态和 API DTO 要分开
- `InterviewState` 只服务于工作流，不要直接暴露给前端

#### 子任务 B：实现面试会话服务

新增文件建议：

- `app/services/interview_service.py`
- `app/api/interview.py`

功能：

- 创建会话
- 获取当前问题
- 提交答案
- 暂存答案
- 完成面试

注意点：

- 先完成会话驱动，不急着做完整评估
- 当前阶段可以先在提交答案时写入占位结果

#### 子任务 C：实现 LangGraph 面试工作流

新增文件建议：

- `app/agent/interview/planner.py`
- `app/agent/interview/executor.py`
- `app/agent/interview/replanner.py`

实现思路：

- `planner` 负责生成问题计划
- `executor` 负责当前题提问与候选答案处理
- `replanner` 负责决定追问、下一题或结束

注意点：

- 不要把整个流程塞进一个节点
- 不要直接在节点内部做复杂数据库写入
- 节点只做推理和状态变更

#### 子任务 D：实现会话缓存

建议实现：

- `InterviewSessionCache` 的 Python 版
- Redis 持有未完成会话状态

注意点：

- Redis 是缓存与运行态加速，不是唯一事实源
- PostgreSQL 仍是最终持久化记录

### 11.4 本阶段验收标准

- 能创建文字面试会话
- 能基于 Skill 生成题目
- 能提交答案并推进流程
- 能触发追问或下一题
- 能在流程结束时落持久化状态

### 11.5 注意要点

- 此阶段先把“流程跑通”，不要急着把评估做满
- 如果同时做追问、报告、导出，很容易失控

---

## 十二、Phase 6：统一评估引擎与报告

### 12.1 阶段目标

建立可复用的评估体系，让文字面试、未来语音面试都能共享同一套评价骨架。

### 12.2 本阶段要实现什么

- 统一评估服务
- 分批评估
- 二次汇总
- reference baseline 注入
- 报告生成

### 12.3 如何实现

#### 子任务 A：实现结构化输出组件

新增文件建议：

- `app/core/structured_output.py`

能力要求：

- 结构化解析
- 失败重试
- 严格 JSON 指令
- 本地 JSON 修复

注意点：

- 这是整个系统最值得复用的 AI 工程组件之一
- 不要把它写死在某个业务 Service 里

#### 子任务 B：实现批量评估器

新增文件建议：

- `app/agent/evaluator/batch_evaluator.py`
- `app/services/evaluation_service.py`

实现内容：

- 将问答记录按批次切分
- 每批独立调用 LLM
- 生成结构化 question evaluations

注意点：

- 单批失败不能拖死整个评估
- 题目数量与评估结果数量必须对齐，不足时补兜底

#### 子任务 C：实现汇总器

新增文件建议：

- `app/agent/evaluator/summarizer.py`

实现内容：

- 基于分批结果生成总结
- 如果汇总失败，回退到批次聚合结果

注意点：

- 汇总是增强，不是唯一结果来源
- 必须有 fallback

#### 子任务 D：报告持久化与导出占位

新增能力：

- `InterviewReportEntity`
- `GET /api/interview/sessions/{id}/report`
- 导出接口占位

注意点：

- 当前阶段可以先生成 JSON/Markdown 报告
- PDF 真正导出可放到下一阶段或后半段

### 12.4 本阶段验收标准

- 可以对完整面试生成结构化报告
- 单批失败仍能给出可读结果
- 报告可查询、可持久化

### 12.5 注意要点

- 报告逻辑必须独立，不要在 `submit_answer` 接口里硬拼长字符串
- 评分标准应外置到 `knowledge_base/rubrics/`

---

## 十三、Phase 7：简历模块

### 13.1 阶段目标

支持简历上传、解析、去重、异步分析，并将分析结果用于个性化出题。

### 13.2 本阶段要实现什么

- 多格式简历上传
- 文本解析
- 内容哈希去重
- 异步分析状态
- 分析结果持久化

### 13.3 如何实现

#### 子任务 A：简历上传与存储

新增文件建议：

- `app/api/resume.py`
- `app/services/resume_service.py`
- `app/services/resume_parse_service.py`
- `app/services/resume_analysis_service.py`

功能：

- 接收文件
- 白名单校验
- 落临时目录或对象存储
- 创建 ResumeEntity

注意点：

- 先别在上传接口里直接同步跑完整 AI 分析
- 上传成功与分析完成要区分

#### 子任务 B：支持 PDF / DOCX / TXT / MD

解析方案：

- 先做最小可用版本
- 如果 Python 生态解析能力不足，可以先支持 TXT / MD / PDF 基础文本提取

注意点：

- 不要因“想一次支持全部格式”拖慢主线
- 可先提供降级说明

#### 子任务 C：异步简历分析

建议实现：

- `PENDING / PROCESSING / COMPLETED / FAILED`
- Redis Stream 或 `arq`

注意点：

- 分析是典型异步任务，适合与上传解耦
- 分析失败要可重试

#### 子任务 D：出题时注入简历信息

在 `question_service.py` 中加入能力：

- 简历文本摘要
- 项目经历关键词
- 技能映射

注意点：

- 不要直接把整份简历无裁剪塞给 LLM
- 先做摘要或结构化提取

### 13.4 本阶段验收标准

- 简历可上传并保存
- 简历可异步分析
- 面试创建时可关联简历
- 出题可体现简历个性化

### 13.5 注意要点

- 先让文字简历分析服务于文字面试，不要同时做语音和报告大联动

---

## 十四、Phase 8：异步任务、多 Provider、限流与系统增强

### 14.1 阶段目标

把系统从“能跑”提升到“更像工程化产品”。

### 14.2 本阶段要实现什么

- Redis Stream 模板
- 多 Provider 运行时切换
- API 限流
- 更完整健康检查
- 安全与可观测性增强

### 14.3 如何实现

#### 子任务 A：异步任务模板化

建议新增：

- `app/core/stream_producer.py`
- `app/core/stream_consumer.py`

典型任务：

- 知识库向量化
- 简历分析
- 面试评估
- PDF 导出

注意点：

- 不要每个任务各写一套 while True 消费逻辑
- 要统一 ACK、重试、失败标记模式

#### 子任务 B：多 Provider 管理

新增文件建议：

- `app/services/provider_service.py`
- `app/api/provider.py`
- `app/repositories/provider_repository.py`

能力：

- 保存 Provider 配置
- 切换默认模型
- 测试连通性

注意点：

- 不要把 Provider 配置只放 `.env`
- 要允许运行时切换

#### 子任务 C：限流中间件

新增文件建议：

- `app/middleware/rate_limit.py`
- `scripts/rate_limit.lua`

能力：

- GLOBAL / IP / USER 维度
- Lua 原子执行
- 统一错误返回

注意点：

- 限流实现一定要可复用，不要写死在某个接口上

### 14.4 本阶段验收标准

- 异步任务有统一模板
- Provider 可配置与切换
- 核心接口具备限流保护
- 健康检查覆盖关键依赖

### 14.5 注意要点

- 这是系统增强阶段，不要反过来阻塞主功能交付
- 如果文字面试主流程还不稳，优先把主流程补稳再做增强

---

## 十五、Phase 9：前端适配与切换

### 15.1 阶段目标

让后端重构成果有可操作的前端入口，至少支撑文字面试、简历上传、知识库问答、Provider 设置。

### 15.2 本阶段要实现什么

- 统一 API 契约
- SSE 事件适配
- 新页面或过渡前端

### 15.3 如何实现

#### 方案 A：保留 `static/` 过渡

适合：

- 先验证主流程
- 低成本联调

注意点：

- 只做最小页面，不要在静态 JS 里继续堆大业务

#### 方案 B：引入独立 `frontend/`

适合：

- 准备长期维护
- 页面模块会明显增长

建议页面：

- 面试中心
- 面试记录
- 简历上传
- 知识库管理
- Provider 设置

### 15.4 本阶段验收标准

- 用户可创建面试并完成答题
- 用户可上传简历
- 用户可查看报告
- 用户可管理知识库

### 15.5 注意要点

- 前端不要反向定义后端字段
- 后端接口稳定后，再大规模做 UI

---

## 十六、Phase 10：收尾、迁移与优化

### 16.1 阶段目标

清理重构遗留、补测试、补文档、压测关键链路，为后续继续迭代打基础。

### 16.2 本阶段要实现什么

- 补齐测试
- 清理旧逻辑依赖
- 补充 README / 部署文档
- 进行简单性能验证
- 评估下一阶段是否接语音面试 / 日程管理

### 16.3 如何实现

#### 子任务 A：清理旧路径耦合

需要确认：

- 是否还有新业务依赖 `agent/aiops/`
- 是否还有逻辑依赖 `MemorySaver` 作为主存储
- 是否还有前端只读 `localStorage`

#### 子任务 B：补测试

重点补：

- Repository
- Service
- LangGraph workflow
- Structured output
- RAG 检索
- 限流

#### 子任务 C：文档收尾

建议补充：

- README：项目功能、启动方式、目录结构
- `.env.example`
- API 概览
- Prompt / Skill 资源说明

### 16.4 本阶段验收标准

- 关键流程有测试覆盖
- 旧样板逻辑不再承载新业务
- 文档能指导新成员或 AI 接手开发

---

## 十七、推荐的 AI 辅助开发任务切分方式

这一部分是为了让你后面“拿着这份路线图指导 AI 编码工作”更顺手。

### 17.1 单次任务模板

建议每次给 AI 的任务格式：

```text
目标阶段：Phase X
本次子任务：XXX
目标文件：
- app/...
- prompts/...

要实现的功能：
1. ...
2. ...

非目标：
1. 不做 ...
2. 不改 ...

验收标准：
1. ...
2. ...

约束：
- 遵守 AGENTS.md
- 不要把新逻辑写进 aiops 模块
- 使用 apply_patch 改文件
- 注释、Docstring 与关键说明尽可能使用中文编写
```

### 17.2 适合 AI 一次完成的粒度

推荐：

- 一个 Repository
- 一个 Service
- 一个 Router
- 一个 LangGraph workflow
- 一个中间件
- 一个技能加载器

不推荐：

- “实现完整面试平台”
- “把所有阶段都做完”
- “把前后端和数据库一次全改”

### 17.3 每轮 AI 编码后的检查清单

每轮完成后，至少检查：

- 代码是否在目标目录
- 是否符合 `AGENTS.md`
- 是否引入了新的硬编码
- 是否破坏旧功能
- 是否写了最小测试或最小验证说明

---

## 十八、建议的阶段执行顺序

如果你准备正式开始重构，我建议按下面顺序推进：

1. Phase 0：目录与配置骨架
2. Phase 1：PostgreSQL / Redis / Storage / 异常处理
3. Phase 2：Entity + Repository + Persistence
4. Phase 3：知识库结构化管理
5. Phase 4：Skill 系统
6. Phase 5：文字面试主流程
7. Phase 6：统一评估引擎
8. Phase 7：简历模块
9. Phase 8：异步任务 / Provider / 限流
10. Phase 9：前端接入
11. Phase 10：清理与收尾

如果中途时间有限，优先保住以下 MVP 路径：

- Knowledge Base
- Skill
- Text Interview
- Evaluation
- Resume Upload

可以延后：

- PDF 精美导出
- 第三方题库 MCP
- 语音面试
- 面试日程管理

---

## 十九、最终交付判断标准

当以下条件同时满足时，可以认为第一阶段重构成功：

1. 面试会话、答案、报告已脱离内存临时态，具备数据库持久化。
2. RAG 不再只是通用聊天工具，而能服务于 Skill 驱动面试。
3. Skill 能通过配置扩展方向，不再需要改核心代码。
4. 评估具备统一结构化输出与可降级能力。
5. 简历、知识库、Provider 都有独立模块和 API。
6. 新业务不再继续堆在 `aiops` 样板路径中。
7. 项目文档足以继续指导 AI 或开发者稳定迭代。

---

## 二十、结语

这次重构的本质不是“把一个 Python 项目改成另一个 Python 项目”，而是：

- 保留现有 `biz_agent` 的底层 Agent / RAG / MCP 优势
- 吸收 Java 面试项目的业务结构、Skill 体系、评估体系和工程经验
- 最终落成一个真正按面试场景设计的 AI 原生应用

后续开发时，建议始终记住一句话：

> 先把底盘搭稳，再把业务搬上去；先让一个阶段闭环，再做下一阶段。
